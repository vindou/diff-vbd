# Progress — feature/infrastructure

| Milestone | Status | Evidence | Commit |
|---|---|---|---|
| M1 — Hertz validation | DONE | barrier: exponent 1.5267, per-delta P/P_theory geomean 1.164; two_stage: exponent 1.4971, geomean 1.145 (gates: exponent +-0.075, geomean within 25% — calibrated by a 3-resolution refinement study, RESULTS.md); KE/PE < 1e-4; load-reaction residual < 5%; d_hat doubling shifts P by 1.06% | this commit |
| M2 — OGC two-stage activation | DONE | C2 at tau / C1 at d_hat pinned by FD (catches the paper's Eq. 19 typo); Hertz re-run under it (M1); convergence/conditioning vs log barrier in RESULTS.md | this commit |
| M3 — Per-vertex conservative bounds | DONE | per-step intersection audits clean (fast impact, no line search, Chebyshev on); negative twin still tunnels; far body keeps >= 95% of its inertial step while a near-contact interface is bound-limited (old filter: ~5%); adversarial review's rigid-row and prescribed-speed findings fixed and pinned by tests; global filter, ACCD and _MIN_USEFUL_TOI deleted | this commit |
| M4 — Static equilibrium + adjoint | DONE | 7 sensitivity classes vs central differences, worst rel err 9.6e-5 (gate 1e-4), barrier active (0 < gap 6.7e-4 < d_hat 1e-3); unconverged adjoint raises | this commit |
| M5 — Setup performance | DONE | element-wise identical outputs (masses bitwise); 231x masses, 41x incidence, 7x surface, 3x coloring on 105K tets | this commit |
| M6 — OGC offset geometry (stretch) | NOT ATTEMPTED | budget went to making M1 honest and verifying M3; a partial contact model is worse than none (DECISIONS.md); what its absence costs is quantified: M2's conditioning win exists only at a large standoff, M3's bounds stay ~0.45 d_hat for resting vertices | — |

# Progress — feature/traced-static

| Milestone | Status | Evidence | Commit |
|---|---|---|---|
| traced-solve + traced-adjoint | DONE | agreement 9.6e-14 (contact, gap 6.7e-4 in band) / 7.1e-14 (no contact) vs 1e-10 gate; vmap B=15 matches host sequential to 5.4e-13 worst; adjoint-under-vmap vs FD: mu 3.9e-6/7.9e-6, radius 3.3e-5/5.0e-6, rest-dir 3.9e-6/4.3e-6 (gate 1e-4); asymmetric refusal: converged=[T,F] → grad[1] all-NaN, grad[0] = solo within 4e-5 (CG-tol class); transcription trace bitwise vs host replica incl. forced rejections + NaN direction; adversarial review: 2 defects found and fixed (FINDINGS.md) | 3ae81b7 |
| batch scaling measured | DONE | random mu draw, plain traced: B=1 7–22x, B=4..64 0.4–2.1x (stall-pocket lanes drag max-over-lanes; laptop run-to-run variance is large, both runs in RESULTS.md); with block-Jacobi: B=4 **21.4x** (4/4 conv), B=16 **7.0x** (16/16), B=64 0.64x (53/64 — pockets under that compilation); GPU untested (linbox01 GPU at 100% util) | this commit |
| portable gates (swept, not pinned) | DONE | pinned fixtures pinned the machine (11/23 failed on x86_64 Linux, all `host.converged` preconditions — loud-fixture design right, pinning conclusion wrong); gates now sweep 27 points, filter on both drivers' `converged` certificates, assert on every survivor, floor >= 8; refusal asymmetry constructed (tol=1e-6 descent-phase lane + penetrating zigzag lane); transcription coverage forced or runtime-selected, never pinned; 16 passed + 66 subtests locally; linbox01 CPU run in flight | this commit |
| pocket characterization (masking bias) | DONE | 135-point grid (mu x indentation x kappa), 5 contexts: stall propensity systematic (~4x with indentation: 4%→15% eager, 4%→11% vmap; with kappa 4%→16% eager), locations a per-compilation lottery (Jaccard 0.00–0.20) — **masking is biased** (drops the stiff end of the load distribution, no symptom); tables in RESULTS.md; bonus finding: certificates don't transfer between compiled programs (B=64 survivors re-batched at B=50: 7 lanes flip to stalls) | this commit |
| block-Jacobi preconditioned CG | DONE | per-vertex 3x3 static-Hessian blocks (elastic incident + exact barrier), PSD-floored, damped like the operator, rebuilt per pass; optional on both CG calls, default off, host driver untouched; prototype: 5/16 pocket stalls -> 0/16 at tol=1e-9; full grid: eager 10.4%→5.9%, vmap 7.4%→9.6% (washes out in the lottery); bench: B=4 21.4x / B=16 7.0x all-converged, B=64 0.64x (11 stalled lanes under that compilation); preconditioned agreement + adjoint sweeps green | 94bf3f7 + this commit |
| caller contract | DONE | in static.py's module docstring: max_iterations is a max-over-lanes price (100 ≈ 8x tax), read `converged` (mean of a poisoned batch is NaN by design, P(≥1 stall) ≈ 99.9% at B=128), do not mask (biased per characterization) — re-solve stalled lanes instead, block_jacobi halves the tax but doesn't repeal the contract, survivor re-batching re-rolls the lottery | this commit |
